const contractAddresses = require("./contractAddresses.json")
const abi = require("./abi.json")

// index.js can be used to represent the entire constants folder which allows to import all objects directly

module.exports = {
    abi,
    contractAddresses,
}
